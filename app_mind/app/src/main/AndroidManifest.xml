<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />

    <application
        android:name=".MindApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:forceDarkAllowed="false"
        android:theme="@style/Theme.Mind"
        tools:targetApi="q">
        <!-- Login is the launcher activity -->
        <activity
            android:name=".auth.ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".auth.ui.RegisterActivity" />
        <activity android:name=".auth.onboarding.OnboardingActivity" />
        <activity android:name=".auth.onboarding.ConsentActivity" />
        <activity android:name=".journal.JournalActivity" />
        <activity android:name=".journal.JournalWriteActivity" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".settings.ui.ProfileActivity" />
        <activity android:name=".schedule.ui.ScheduleActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".checkin.ui.Phq2Activity" />
        <activity android:name=".checkin.ui.Phq9Activity" />
        <activity android:name=".checkin.ui.PhqResultActivity" />
        <activity android:name=".vr.VideoRecordActivity" />
        <activity android:name=".vr.PhotoCaptureActivity" />

        <!-- FCM Service nhận push notification -->
        <service
            android:name=".notification.MindyMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>